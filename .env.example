# 复制成 .env 后填写。.env 已在 .gitignore 里，不会被提交。
#
#   cp .env.example .env && vim .env
#   set -a && source .env && set +a
#   python3 cli.py doctor

# ---------- SocialDataX ----------
# 申请/管理：https://socialdatax.com
SOCIALDATAX_API_KEY=

# ---------- 飞书自建应用 ----------
# 开放平台建企业自建应用 → 开权限 bitable:app → 发布版本
FEISHU_APP_ID=
FEISHU_APP_SECRET=

# 多维表格地址 https://xxx.feishu.cn/base/<app_token>?table=<table_id>
FEISHU_APP_TOKEN=
FEISHU_TABLE_ID=

# ---------- 可选调参 ----------
# 并发上限。官方 skill 要求最多 3，默认取 2 留一档余量。
MAX_CONCURRENCY=2

# 小红书笔记发布多少天内额外调一次 detail 拿点赞/收藏。
# 设成 0 = 完全不调 detail，月成本立刻少三分之一，代价是爆文只能靠评论数判。
DETAIL_WITHIN_DAYS=7

# 单次运行的软截止（秒）。独立服务跑批量设 0（不限）；
# 扣子代码节点有 60 秒硬上限，那边设 45。
SOFT_DEADLINE_SECONDS=0
