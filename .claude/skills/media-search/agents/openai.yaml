interface:
  display_name: "Media Search"
  short_description: "Search content by keyword"
  default_prompt: "Use $media-search to search supported social media content by keyword across XHS, Douyin, Kuaishou, Weibo, and WeChat Channels."

policy:
  allow_implicit_invocation: true
